extends layout

block stylesheet
  link(rel='stylesheet',type='text/css',href='/css/mailpipe-gate.css')

block content
  .container
    .row
      .span2.offset3.login-box
        .icon
          img(src='/images/icons/Mail.png',alt='Welcome to MailPipe')
          h4
            | Welcome to 
            small MailPipe
      .span5
        
        form.login-form(action='/users/login',method='post')
          if flash.error && flash.error.length > 0
            .alert.alert-error
              = flash.error[0]
          if flash.info
            .alert.alert-success
              = flash.info
          .control-group
            input#username(type='text',placeholder='Username',name='username')
            i.input-icon.fui-user
          .control-group
            input#password(type='password',placeholder='Password',name='password')
            i.input-icon.fui-lock
          input(type='hidden',name='_csrf',value='#{token}')
          button.btn.btn-primary.btn-large.btn-block(type='submit') Login
          a.login-link(href='/users/forget.html') Lost your password?
